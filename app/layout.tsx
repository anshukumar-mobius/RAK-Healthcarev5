import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'RAK Hospital - AI Decision Support',
  description: 'Transforming RAK Hospital\'s Clinical Excellence with AI-Powered Decision Support',
  keywords: 'healthcare, AI, clinical decision support, RAK Hospital, patient safety',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.className} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}